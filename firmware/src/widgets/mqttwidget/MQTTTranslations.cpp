#include "MQTTTranslations.h"

// Languages are defined in I18n.h:
// EN, DE, FR

// All translation variables for MQTTWidget should start with "t_mqtt"

constexpr Translation t_mqttHost = {
    "MQTT Host", // EN
};

constexpr Translation t_mqttPort = {
    "MQTT Port", // EN
};

constexpr Translation t_mqttSetupTopic = {
    "MQTT Setup Topic", // EN
};

constexpr Translation t_mqttUser = {
    "MQTT user (empty if authentication is not required)", // EN
};

constexpr Translation t_mqttPass = {
    "MQTT pass (empty if authentication is not required)", // EN
};